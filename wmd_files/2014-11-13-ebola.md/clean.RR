load('read.RData')
pdf("clean.Rout.pdf")
# End RR preface
# Global Environment: read.RData

# Scripts: clean.R

class(data$Date)
data <- within(data, {
	Date <- as.Date(Date, format="%Y-%m-%d")
	Value <- as.numeric(as.character(Value))
})

national <- subset(data, Localite=="National")

print(summary(data))

# Begin RR postscript
# If you see this in an R log, your R script did not close properly
save.image(file="clean.RData")
