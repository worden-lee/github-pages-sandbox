load('nomogram.RData')
pdf("example.Rout.pdf")
# End RR preface
# Global Environment: nomogram.RData

# Scripts: example.R

eqLines <- function(a, b){
	abline(a, b-a, lty=2, lwd=1.5)
	abline(-a, b+a, lty=3, lwd=1.5)
}
nomogram()
eqLines(-3, -5)

# Begin RR postscript
# If you see this in an R log, your R script did not close properly
save.image(file="example.RData")
